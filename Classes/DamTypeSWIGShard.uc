class DamTypeSWIGShard extends WeaponDamageType;

defaultproperties
{
     WeaponClass=Class'mm_SWXWeapons.SWIG'
     DeathString="%o got %k's pointy icey!"
     FemaleSuicide="%o got kill by own ice shard even if you can't."
     MaleSuicide="%o got kill by own ice shard even if you can't."
}
